name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build bundles
        run: ./scripts/build.sh

      - name: Script smoke tests
        run: ./scripts/test_scripts.sh
